import UIKit

struct s {
    
    // MARK: Titles
    static let map = "Map"
    static let details = "Details"
    static let listView = "List View"
    static let login = "Login"
    
    // MARK: Common
    static let failed = "Failed"
    
    // MARK: Buttons
    static let ok = "Ok"
    
    // MARK: Success Messages
    static let loggedInSuccessfully = "Logged in Successfully"
    
    // MARK: Error Messages
    static let userCanceledLogin = "User canceled login"
    static let profileDataNotFound = "Profile data not found"
}

struct Asserts {
    static let circleFill = UIImage(systemName: "location.circle.fill")!
    static let paperClip = UIImage(systemName: "paperclip")!
}
